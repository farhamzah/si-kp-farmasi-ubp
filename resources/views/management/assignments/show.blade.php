@extends('layouts.app')
@section('title','Detail Penempatan KP - '.config('app.name'))
@section('page_title','Detail Penempatan KP')
@section('content')
<div class="grid gap-5 xl:grid-cols-[0.9fr_1.1fr]">@if(session('status'))<div class="xl:col-span-2 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif @if($errors->any())<div class="xl:col-span-2 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif
<section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><span class="rounded-full {{ $assignment->statusBadgeClass() }} px-3 py-1 text-xs font-semibold">{{ $assignment->statusLabel() }}</span><h2 class="mt-4 text-2xl font-bold">{{ $assignment->student->user->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $assignment->student->nim ?: '-' }} · {{ $assignment->period->name }}</p><div class="mt-6 space-y-3 text-sm"><div class="rounded-lg bg-slate-50 p-3"><p class="text-xs text-slate-500">Tempat KP</p><p class="font-bold">{{ $assignment->place->name }}</p></div><div class="rounded-lg bg-slate-50 p-3"><p class="text-xs text-slate-500">Pembimbing Dalam</p><p class="font-bold">{{ $assignment->internalSupervisor?->user?->name ?? 'Belum ada' }}</p></div><div class="rounded-lg bg-slate-50 p-3"><p class="text-xs text-slate-500">Pembimbing Lapangan</p><p class="font-bold">{{ $assignment->fieldSupervisor?->user?->name ?? 'Belum ada' }}</p></div></div><div class="mt-5 flex gap-2"><a href="{{ route('management.kp-assignments.edit',$assignment) }}" class="rounded-lg bg-teal-600 px-4 py-2 text-sm font-semibold text-white">Edit Pembimbing</a>@if($assignment->status !== 'dibatalkan')<form method="POST" action="{{ route('management.kp-assignments.cancel',$assignment) }}" onsubmit="return confirm('Batalkan penempatan ini?')">@csrf<input type="hidden" name="reason" value="Dibatalkan dari halaman detail."><button class="rounded-lg border border-rose-200 px-4 py-2 text-sm font-semibold text-rose-700">Batalkan</button></form>@endif</div></section>
<section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h3 class="text-lg font-bold">Riwayat Log</h3><div class="mt-4 space-y-2">@forelse($assignment->logs->sortByDesc('created_at') as $log)<div class="rounded-lg border border-slate-200 p-3 text-sm"><strong>{{ str_replace('_',' ',ucfirst($log->action)) }}</strong><div class="text-xs text-slate-500">{{ $log->user?->name ?? 'Sistem' }} · {{ $log->created_at->format('d M Y H:i') }}</div>@if($log->note)<p class="mt-1 text-slate-600">{{ $log->note }}</p>@endif</div>@empty<p class="rounded-lg bg-slate-50 px-4 py-3 text-sm text-slate-500">Belum ada log.</p>@endforelse</div></section></div>
@endsection
