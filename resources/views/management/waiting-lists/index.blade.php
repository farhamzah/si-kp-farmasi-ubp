@extends('layouts.app')
@section('title', 'Daftar Tunggu - '.config('app.name'))
@section('page_title', 'Daftar Tunggu')
@section('content')
<div class="space-y-5">@if(session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif
<section class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><form method="GET" class="grid gap-3 md:grid-cols-[1fr_220px_180px_auto]"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Cari mahasiswa/NIM" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><select name="period" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Semua Periode</option>@foreach($periods as $period)<option value="{{ $period->id }}" @selected(($filters['period'] ?? '') == $period->id)>{{ $period->name }}</option>@endforeach</select><select name="status" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Semua Status</option><option value="menunggu">Menunggu</option><option value="sudah_memilih">Sudah Memilih</option><option value="dibatalkan">Dibatalkan</option></select><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Filter</button></form></section>
<section class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-4 py-3">Mahasiswa</th><th class="px-4 py-3">Periode</th><th class="px-4 py-3">Masuk</th><th class="px-4 py-3">Status</th><th class="px-4 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($waitingLists as $waiting)<tr><td class="px-4 py-4"><div class="font-semibold">{{ $waiting->student->user->name }}</div><div class="text-xs text-slate-500">{{ $waiting->student->nim ?: '-' }}</div></td><td class="px-4 py-4">{{ $waiting->period->name }}</td><td class="px-4 py-4">{{ $waiting->joined_at?->format('d M Y H:i') }}</td><td class="px-4 py-4"><span class="rounded-full {{ $waiting->statusBadgeClass() }} px-2 py-1 text-xs font-semibold">{{ $waiting->statusLabel() }}</span></td><td class="px-4 py-4 text-right">@if($waiting->status === 'menunggu')<form method="POST" action="{{ route('management.waiting-lists.cancel', $waiting) }}" onsubmit="return confirm('Batalkan daftar tunggu ini?')">@csrf<button class="rounded-lg border border-rose-200 px-3 py-1.5 text-xs font-semibold text-rose-700">Batalkan</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="px-4 py-10 text-center text-slate-500">Belum ada daftar tunggu.</td></tr>@endforelse</tbody></table></div><div class="border-t px-4 py-3">{{ $waitingLists->links() }}</div></section></div>
@endsection
