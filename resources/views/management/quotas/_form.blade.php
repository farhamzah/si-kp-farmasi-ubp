@csrf
@if($quota->exists) @method('PUT') @endif
<section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div class="grid gap-4 md:grid-cols-2"><div><label class="text-sm font-semibold text-slate-700">Periode KP</label><select name="kp_period_id" class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Pilih periode</option>@foreach($periods as $period)<option value="{{ $period->id }}" @selected(old('kp_period_id', $quota->kp_period_id) == $period->id)>{{ $period->name }}</option>@endforeach</select>@error('kp_period_id')<p class="mt-1 text-xs text-red-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-semibold text-slate-700">Tempat KP</label><select name="kp_place_id" class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Pilih tempat</option>@foreach($places as $place)<option value="{{ $place->id }}" @selected(old('kp_place_id', $quota->kp_place_id) == $place->id)>{{ $place->name }} - {{ $place->typeLabel() }}</option>@endforeach</select>@error('kp_place_id')<p class="mt-1 text-xs text-red-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-semibold text-slate-700">Kuota</label><input type="number" min="0" name="quota" value="{{ old('quota', $quota->quota ?? 0) }}" class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm">@error('quota')<p class="mt-1 text-xs text-red-600">{{ $message }}</p>@enderror</div><label class="mt-8 flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" name="is_open" value="1" @checked(old('is_open', $quota->is_open ?? true)) class="rounded border-slate-300 text-teal-600"> Kuota dibuka</label><div class="md:col-span-2"><label class="text-sm font-semibold text-slate-700">Catatan</label><textarea name="notes" rows="4" class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm">{{ old('notes', $quota->notes) }}</textarea><p class="mt-2 text-xs text-slate-500">Kuota tidak boleh dikurangi lebih kecil dari jumlah mahasiswa yang sudah memilih. Tahap 3 masih menghitung terisi = 0.</p></div></div><div class="mt-6 flex justify-end gap-2"><a href="{{ route('management.kp-place-quotas.index') }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700">Batal</a><button class="rounded-lg bg-teal-600 px-4 py-2 text-sm font-semibold text-white">Simpan Kuota</button></div></section>
