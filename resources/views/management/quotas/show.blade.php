@extends('layouts.app')
@section('title', 'Detail Kuota Tempat KP - '.config('app.name'))
@section('page_title', 'Detail Kuota Tempat KP')
@section('content')
<div class="grid gap-5 xl:grid-cols-[0.8fr_1.2fr]"><section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><span class="rounded-full {{ $quota->statusBadgeClass() }} px-3 py-1 text-xs font-semibold">{{ $quota->statusLabel() }}</span><h2 class="mt-4 text-2xl font-bold text-slate-950">{{ $quota->place->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $quota->period->name }}</p><div class="mt-6 grid grid-cols-3 gap-3 text-center"><div class="rounded-lg bg-slate-50 p-3"><p class="text-xs text-slate-500">Kuota</p><p class="text-xl font-bold">{{ $quota->quota }}</p></div><div class="rounded-lg bg-slate-50 p-3"><p class="text-xs text-slate-500">Terisi</p><p class="text-xl font-bold">{{ $quota->filledCount() }}</p></div><div class="rounded-lg bg-slate-50 p-3"><p class="text-xs text-slate-500">Sisa</p><p class="text-xl font-bold">{{ $quota->remainingQuota() }}</p></div></div><div class="mt-6 flex gap-2"><a href="{{ route('management.kp-place-quotas.edit', $quota) }}" class="rounded-lg bg-teal-600 px-4 py-2 text-sm font-semibold text-white">Edit</a><form method="POST" action="{{ route('management.kp-place-quotas.destroy', $quota) }}" onsubmit="return confirm('Hapus kuota ini?')">@csrf @method('DELETE')<button class="rounded-lg border border-rose-200 px-4 py-2 text-sm font-semibold text-rose-700">Hapus</button></form></div></section><section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h3 class="text-lg font-bold text-slate-950">Log Terbaru</h3><div class="mt-4 space-y-2">@forelse($quota->logs as $log)<div class="rounded-lg border border-slate-200 p-3 text-sm"><strong>{{ $log->action }}</strong> oleh {{ $log->user?->name ?? '-' }}<div class="text-xs text-slate-500">{{ $log->created_at->format('d M Y H:i') }} - {{ $log->note ?: '-' }}</div></div>@empty<p class="rounded-lg bg-slate-50 px-4 py-3 text-sm text-slate-500">Belum ada log.</p>@endforelse</div></section></div>
@endsection
