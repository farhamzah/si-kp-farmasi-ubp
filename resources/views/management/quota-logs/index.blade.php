@extends('layouts.app')
@section('title', 'Log Kuota - '.config('app.name'))
@section('page_title', 'Log Kuota')
@section('content')
<div class="space-y-5"><div class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><form method="GET" class="grid gap-3 md:grid-cols-[1fr_1fr_auto]"><div><label class="text-xs font-semibold uppercase tracking-wide text-slate-500">Periode</label><select name="period" class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Semua</option>@foreach($periods as $period)<option value="{{ $period->id }}" @selected(($filters['period'] ?? '') == $period->id)>{{ $period->name }}</option>@endforeach</select></div><div><label class="text-xs font-semibold uppercase tracking-wide text-slate-500">Tempat</label><select name="place" class="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Semua</option>@foreach($places as $place)<option value="{{ $place->id }}" @selected(($filters['place'] ?? '') == $place->id)>{{ $place->name }}</option>@endforeach</select></div><button class="self-end rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Filter</button></form></div><div class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-4 py-3">Waktu</th><th class="px-4 py-3">User</th><th class="px-4 py-3">Periode/Tempat</th><th class="px-4 py-3">Action</th><th class="px-4 py-3">Perubahan</th><th class="px-4 py-3">Catatan</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($logs as $log)<tr><td class="px-4 py-4">{{ $log->created_at->format('d M Y H:i') }}</td><td class="px-4 py-4">{{ $log->user?->name ?? '-' }}</td><td class="px-4 py-4">{{ $log->quota?->period?->name ?? '-' }}<br><span class="text-xs text-slate-500">{{ $log->quota?->place?->name ?? '-' }}</span></td><td class="px-4 py-4"><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-700">{{ $log->action }}</span></td><td class="px-4 py-4">Kuota: {{ $log->old_quota ?? '-' }} -> {{ $log->new_quota ?? '-' }}<br>Status: {{ is_null($log->old_is_open) ? '-' : ($log->old_is_open ? 'Buka' : 'Tutup') }} -> {{ is_null($log->new_is_open) ? '-' : ($log->new_is_open ? 'Buka' : 'Tutup') }}</td><td class="px-4 py-4">{{ $log->note ?: '-' }}</td></tr>@empty<tr><td colspan="6" class="px-4 py-10 text-center text-slate-500">Belum ada log kuota.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-200 px-4 py-3">{{ $logs->links() }}</div></div></div>
@endsection
