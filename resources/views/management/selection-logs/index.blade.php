@extends('layouts.app')
@section('title', 'Log Pemilihan - '.config('app.name'))
@section('page_title', 'Log Pemilihan')
@section('content')
<div class="space-y-5"><section class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><form method="GET" class="grid gap-3 md:grid-cols-[1fr_220px_160px_auto]"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Cari action/pesan" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><select name="period" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Semua Periode</option>@foreach($periods as $period)<option value="{{ $period->id }}" @selected(($filters['period'] ?? '') == $period->id)>{{ $period->name }}</option>@endforeach</select><select name="status" class="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">Semua</option><option value="success">Success</option><option value="failed">Failed</option><option value="info">Info</option></select><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Filter</button></form></section><section class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-4 py-3">Waktu</th><th class="px-4 py-3">User</th><th class="px-4 py-3">Mahasiswa</th><th class="px-4 py-3">Tempat</th><th class="px-4 py-3">Action</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Pesan</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($logs as $log)<tr><td class="px-4 py-4">{{ $log->created_at->format('d M Y H:i') }}</td><td class="px-4 py-4">{{ $log->user?->name ?? 'Sistem' }}</td><td class="px-4 py-4">{{ $log->student?->user?->name ?? '-' }}</td><td class="px-4 py-4">{{ $log->place?->name ?? '-' }}</td><td class="px-4 py-4 font-semibold">{{ $log->action }}</td><td class="px-4 py-4"><span class="rounded-full {{ $log->status === 'success' ? 'bg-emerald-50 text-emerald-700' : ($log->status === 'failed' ? 'bg-rose-50 text-rose-700' : 'bg-sky-50 text-sky-700') }} px-2 py-1 text-xs font-semibold">{{ $log->status }}</span></td><td class="px-4 py-4">{{ $log->message }}</td></tr>@empty<tr><td colspan="7" class="px-4 py-10 text-center text-slate-500">Belum ada log pemilihan.</td></tr>@endforelse</tbody></table></div><div class="border-t px-4 py-3">{{ $logs->links() }}</div></section></div>
@endsection
